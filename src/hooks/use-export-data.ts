// Stub — data export feature not yet implemented
export function useExportData(_userId?: string) {
  const loading = false;
  const error: string | null = null;
  const success = false;

  const exportData = async () => {};

  return { loading, error, success, exportData };
}
